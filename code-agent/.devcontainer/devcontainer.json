{
  "name": "Ubuntu + Homebrew",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "VARIANT": "22.04",
      "USERNAME": "vscode",
      "BREW_PACKAGES": "",
      "CLAUDE_VERSION": "2.0.1",
      "CODEX_VERSION": "0.42.0",
      "NODE_VERSION": "22.20.0",
      "GEMINI_CLI_VERSION": "0.7.0",
      "BUN_VERSION": "bun-v1.2.23",
      "UV_VERSION": "0.8.14",
      "SERENA_REPO": "https://github.com/oraios/serena",
      "SERENA_TAG": "v0.1.4",
      "TIMEZONE": "Asia/Tokyo"
    }
  },
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=delegated",
  "remoteUser": "vscode",
  "remoteEnv": { "SHELL": "/usr/bin/zsh", "TZ": "Asia/Tokyo" },
  "mounts": [
    "source=claude-code-bashhistory-${devcontainerId},target=/commandhistory,type=volume",
    "source=claude-code-config-${devcontainerId},target=/home/vscode/.claude,type=volume",
    "source=codex-code-config-${devcontainerId},target=/home/vscode/.codex,type=volume",
    "source=gemini-code-config-${devcontainerId},target=/home/vscode/.gemini,type=volume",
    "source=${localWorkspaceFolder}/.devcontainer/.zshrc.local,target=/home/vscode/.zshrc.local,type=bind,consistency=cached"
  ],
  "updateRemoteUserUID": true,
  "features": {},
  "postCreateCommand": "brew --version || true; gh --version || true; claude --version || true; codex --version || true; node -v || true; npm -v || true; bun --version || true; gemini --version || true; uv --version || true; command -v serena-mcp-server || true",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.profiles.linux": {
          "zsh": {
            "path": "/usr/bin/zsh"
          }
        },
        "terminal.integrated.fontFamily": "MesloLGS NF"
      },
      "extensions": [
        "ms-azuretools.vscode-docker",
        "mkhl.direnv"
      ]
    }
  },
  "containerEnv": {
    "POWERLEVEL9K_DISABLE_CONFIGURATION_WIZARD": "true",
    "CLAUDE_CONFIG_DIR": "/home/vscode/.claude"
  }
}
